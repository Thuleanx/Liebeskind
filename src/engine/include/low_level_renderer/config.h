#pragma once

#include <cstdint>

#include "low_level_renderer/bloom.h"

namespace graphics {
constexpr uint32_t MAX_FRAMES_IN_FLIGHT = 3;
constexpr BloomSettings BLOOM_SETTINGS = {};
}  // namespace graphics
